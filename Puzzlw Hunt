#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;

    if (N >= 6 && N <= 8)
        cout << "Yes" << endl;  // Eligible
    else
        cout << "No" << endl;   // Not eligible

    return 0;
}
